name: release

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest

    steps:
      - name: 1) Checkout repository
        uses: actions/checkout@v4

      - name: 2) Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 3) Validate module metadata
        run: node scripts/validate-module.mjs

      - name: 4) Validate module.json version matches tag
        if: startsWith(github.ref, 'refs/tags/v')
        run: |
          TAG_VERSION="${GITHUB_REF_NAME#v}"
          MODULE_VERSION=$(node -p "JSON.parse(require('fs').readFileSync('module.json','utf8')).version")
          echo "Tag version:    ${TAG_VERSION}"
          echo "Module version: ${MODULE_VERSION}"
          test "${TAG_VERSION}" = "${MODULE_VERSION}"

      - name: 5) Prepare Forge package contents
        run: node scripts/prepare-package.mjs --output release

      - name: 6) Build module.zip artifact
        run: |
          cd release/staging
          zip -r ../module.zip .

      - name: 7) Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: party-operations-package
          path: |
            release/module.zip
            release/module.json

      - name: 8) Create GitHub release with module artifacts
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            release/module.zip
            release/module.json
          generate_release_notes: true
