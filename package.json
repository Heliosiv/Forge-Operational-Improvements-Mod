{
  "name": "party-operations-tooling",
  "private": true,
  "version": "1.0.0",
  "description": "Local tooling for Party Operations build and data sync scripts.",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "check:drift": "node scripts/check-drift.js"
  },
  "dependencies": {
    "classic-level": "3.0.0"
  }
}
